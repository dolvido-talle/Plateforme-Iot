/* 
import { BrowserRouter as Router, Switch, Route } from "react-router-dom";
import Login from "../pages/login";
import ProtectedRouteAuth from "./guards/ProtectedRouteAuth";

const Routes = () => {
  return (
    <Router>
      <Switch>
        <Route path="/login" component={Login} />
        <ProtectedRouteAuth path="/devices" component={DevicePage} />
      </Switch>
    </Router>
  );
};

export default Routes;
 */
